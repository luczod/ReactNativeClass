// import React, { useState } from "react";
import { View, Button } from "react-native";
// npx expo start --clear

export function BtnBox() {
  return (
    <View className="mt-10">
      <Button title="Clique aqui 4" color={"red"}></Button>
    </View>
  );
}
